pipeline {
  agent {label 'linux'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          echo $PATH
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
