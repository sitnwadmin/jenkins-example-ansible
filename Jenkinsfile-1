pipeline {
  agent {label 'linux'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          echo $PATH
          ansible-galaxy-3 collection install paloaltonetworks.panos
        '''
      }
    }
  }
}
